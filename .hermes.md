# Hermes project context

Default behavior for sessions in this repository:

1) Ensure `caveman` skill is available.
   - Skill source URL: https://github.com/JuliusBrussee/caveman/
   - If missing, install first (pinned identifier):
     - `hermes skills tap add https://github.com/JuliusBrussee/caveman/`
     - `printf 'y\n' | hermes skills install skills-sh/juliusbrussee/caveman/caveman`

2) Load `caveman` at session start.
3) Use caveman style by default unless the user asks for normal mode.
4) Keep technical terms, code, and error text exact.
5) Hermes version upgrade guardrail: after any `HERMES_AGENT_VERSION` bump, explicitly verify whether upstream fixed OAuth service-context first-run detection (`_has_any_provider_configured`), then remove local tg-gateway config workaround only after validation.
